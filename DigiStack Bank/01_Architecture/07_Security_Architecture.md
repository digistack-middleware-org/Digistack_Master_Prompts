ID: ARCHVIZ07
Version: 0.1 (scaffold)
Status: Not Populated

Title: Security Architecture

Imports:
STD (§Security)
RACI01 (Security Team role)

Populate At: P01 v10 (roles/registry), extended v11/v12 (SSL/mTLS)

---

## What this will show
Customer/Administrator roles, file registry, SSL hops (browser→IHS,
IHS→AppServer, AppServer→DB), which hop carries mTLS.

## Status
Not yet populated. Security features don't exist until v10-v12.